import { SectionShell } from './SectionShell'

export function BattersBoxSection() {
  return (
    <SectionShell index={3} title="Batter's Box (Setups)">
      <p className="db-coming">
        Coming next — buy-at-LRR / trim-at-TRR / exit setups with exact levels
      </p>
    </SectionShell>
  )
}
